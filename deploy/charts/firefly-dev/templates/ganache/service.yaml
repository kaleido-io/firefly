apiVersion: v1
kind: Service
metadata:
  name: {{ include "firefly-dev.fullname" . }}-ganache
  labels:
  {{- include "firefly-dev.labels" . | nindent 4 }}
spec:
  type: {{ .Values.ganache.service.type }}
  ports:
    - port: {{ .Values.ganache.service.port }}
      targetPort: geth
      protocol: TCP
      name: geth
  selector:
    {{- include "firefly-dev.selectorLabels" . | nindent 4 }}
