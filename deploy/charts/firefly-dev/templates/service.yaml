apiVersion: v1
kind: Service
metadata:
  name: {{ include "firefly-dev.fullname" . }}
  labels:
    {{- include "firefly-dev.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "firefly-dev.selectorLabels" . | nindent 4 }}
